# PYRA EA

Android app for https://red-gem-scanner.lovable.app, generated automatically.

The build runs on GitHub Actions and publishes `app-debug.apk` under the
**apk** release. Download it on your Android phone and open it to install
(allow installs from unknown sources when asked).
